public class U1 extends Rocket{

    public U1(int rocketCost, int rocketWeightKg, int maxWeightWithCargoKg, double chanceLaunchExplosion, double chanceLandingCrash) {
        super(rocketCost, rocketWeightKg, maxWeightWithCargoKg, chanceLaunchExplosion, chanceLandingCrash);
    }
                        //  5% * (cargo carried / cargo limit)
                       //  1% * (cargo carried / cargo limit)*/

    @Override
    public boolean launch() {

        return super.launch();
    }

    @Override
    public boolean land() {
        return super.land();
    }



/*    int rocketCost = 100;
    int rocketWeightKg = 10000;
    int maxWeightWithCargoKg = 18000;
    double chanceLaunchExplosion;            //  5% * (cargo carried / cargo limit)
    double chanceLandingCrash;                    //  1% * (cargo carried / cargo limit)*/

/*    Create classes U1 and U2 that extend the Rocket class and override the land and launch methods to calculate the corresponding chance of exploding and return either true or false based on a random number using the probability equation for each.*/
//Utwórz klasy U1 i U2, które rozszerzają klasę Rakieta i zastępują metody lądowania i wystrzeliwania, aby obliczyć odpowiednią szansę na eksplozję i zwrócić prawdę lub fałsz na podstawie liczby losowej przy użyciu równania prawdopodobieństwa dla każdej z nich.

}
